query getProducts($filter: InputProductsFilter) {
    products(filter: $filter) {
        id
        imageUrl
        stock
        price
        title {
            en
        }
        color {
            value
        }
    }
}
