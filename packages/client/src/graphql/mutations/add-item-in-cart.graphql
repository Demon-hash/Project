mutation AddItemInCart($id: ID!, $products: [InputAddCartProduct]!) {
    addCartProducts(id: $id, products: $products) {
        id
        products {
            id
            count
        }
    }
}
